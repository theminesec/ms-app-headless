<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Disabled -->
    <item android:alpha="@dimen/material_emphasis_disabled" android:color="?colorOnSurface" android:state_enabled="false" />

    <!-- Focused -->
    <item android:color="?colorInput" android:state_focused="true" />

    <!-- Hovered -->
    <item android:color="?colorInput" android:state_hovered="true" />

    <!-- Normal -->
    <item android:color="?colorInput" />
</selector>
